# Build artifacts (local dev builds — not for image)
/mini-s3
/mini-s3.exe
*.exe

# The image uses pre-built binaries staged at binaries/ by the release
# workflow. Ignore everything else under go/ to keep the build context small.
*
!binaries/
!Dockerfile
